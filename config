# the below lines need to be copied to .git/config
# this helps in maintiaining local id for each project
# when two or more members are using the computer and git
# also credentials.helper is set to empty in ~\.gitconfig
# so that every time you run 'git push' it will ask the user
# to pick and choose github account
[user]
    name = Vineel Kovvuri
    email = vineel.kovvuri@gmail.com
